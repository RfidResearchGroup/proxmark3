# Docker tests

Usage:
- enter one of the subfolders
- `../build.sh` to build the docker image
- `../run.sh` to run a container and get a shell
- `../run.sh tests` to run a container and execute its `run_tests.sh`
