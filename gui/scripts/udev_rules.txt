SUBSYSTEM=="usb", ATTR{idVendor}=="0483", ATTR{idProduct}=="5740", MODE:="0666", GROUP:="plugdev", TAG+="uaccess"

# Example: Proxmark3 RDV4 vendor/product may vary; adjust idVendor/idProduct as needed.
# Save this file as /etc/udev/rules.d/99-proxmark3.rules (requires sudo) then run:
#   sudo udevadm control --reload-rules && sudo udevadm trigger
